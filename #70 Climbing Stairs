class Solution(object):
    def climbStairs(self, n):
        """
        :type n: int
        :rtype: int
        """
        pre = 1
        curr = 1
        for i in range(1, n) :
            curr, pre = pre+curr, curr
        return curr
